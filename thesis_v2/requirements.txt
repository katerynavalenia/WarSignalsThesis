# Minimal dependencies for Phase 0–1 (project setup and financial-data audit)
# Heavy ML and NLP dependencies will be added in later phases.

# Core data processing
pandas>=2.0
numpy>=1.24
pyarrow>=14.0          # Parquet support
openpyxl>=3.1          # Bloomberg .xlsx
xlrd>=2.0              # Bloomberg .xls (GPR data)

# Configuration
pyyaml>=6.0

# Notebooks
jupyter>=1.0
ipykernel>=6.0

# Visualization (for audit charts)
matplotlib>=3.7
seaborn>=0.12

# Testing
pytest>=7.0

# ── Phase 3 — GDELT extraction (run in Colab) ──
requests>=2.30        # GDELT API calls (network)
datasketch>=1.10      # MinHash/LSH dedup (high RAM)
langdetect>=1.0.9     # Language detection
tqdm>=4.65            # Progress bars

# ── Phase 5+ dependencies (added for Phase 5 merge & feature engineering) ──
statsmodels>=0.14     # econometric baselines + AR(1) attack-surprise hook
scikit-learn>=1.3     # preprocessing, evaluation metrics, lag features
arch>=6.2             # GARCH-family volatility baselines (Phase 6)

# ── Phase 7 — ML models (gradient boosting + SHAP) ──
xgboost>=2.0          # Phase 7 principal boosting algorithm (decision 2026-07-01)
shap>=0.44            # SHAP TreeExplainer for XGBoost feature attribution
# lightgbm is commented: XGBoost is the principal algorithm; LightGBM kept
# as a documented robustness fallback only (decision_log 2026-07-01).

# ── Phase 4 Tier 2 NLP (deferred until after first milestone) ──
# transformers         # multilingual NLP — Colab GPU
# torch                # transformer backend — Colab GPU

# ── v3 — panel econometrics, source matching, long-sample data ──
linearmodels>=6.0        # PanelOLS with entity+time effects and clustered SEs
rapidfuzz>=3.0           # SIPRI company-name → ticker matching
yfinance>=0.2            # long-history defence basket + market controls back to 2015

# ── v3 — GDELT ingestion via BigQuery (see docs/v3/environment_setup.md §3.2) ──
# The 4.19 TB translingual archive is aggregated server-side; only a few MB of
# daily series come back. Needs GCP_SA_KEY_B64 + GCP_PROJECT in the environment.
google-cloud-bigquery>=3.20
db-dtypes>=1.2           # BigQuery → pandas type mapping
google-api-python-client>=2.100   # Drive API, if Drive access is configured
