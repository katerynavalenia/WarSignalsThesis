# The submission draft

**This is the thesis to compile and submit.** One source file, one command:

```bash
cd thesis/final && ./build.sh      # writes thesis.pdf beside the source
```

The directory is self-contained — `thesis.tex`, `references.bib` and
`figures/` are everything the document needs. It can be zipped and uploaded to
Overleaf as-is, with `thesis.tex` set as the main file.

Verified build: **32 pages, 7 tables, 3 figures, no overfull boxes, and no
unresolved citations or cross-references**, with both tectonic 0.17 and Debian
TeX Live 2025. `build.sh` tries `tectonic`, then `latexmk`, then a three-pass
`pdflatex` + `bibtex`, and warns if any `[?]` marker survives into the PDF.

The title and abstract share page one, and there is no table of contents. The
abstract is set single-spaced to fit; the body stays one-and-a-half spaced.

**On a Debian or Ubuntu TeX Live install** the document needs one package
beyond `texlive-latex-recommended`, because every table uses `threeparttable`
for its notes:

```bash
sudo apt install texlive-latex-extra    # ~112 MB
```

Nothing else is required. The euro sign uses `\texteuro` from the kernel
rather than the `eurosym` package, specifically so that the roughly 1 GB
`texlive-fonts-extra` set is not a dependency of one character.

Section balance against the brief: Introduction pp. 2–5 (the brief asks for
about four), Data and Identification pp. 6–9, Descriptive Statistics
pp. 10–17, Empirical Findings pp. 18–27, Conclusion pp. 28–30, References
pp. 31–32.

## What supersedes what

| file | status |
|---|---|
| `thesis/final/thesis.tex` | **the submission draft** |
| `thesis/latex/main.tex` → `Master_Thesis_3.pdf` | the earlier research question (*War Signals and Defense Equity Risk*: physical attacks, SHAP, MAE horse race). Kept because the history is cited, not for submission |
| `Master_Thesis_2.pdf` (repo root) | built from the deleted `thesis_v2.tex`, and superseded by this directory |
| `Master thesis.txt` (repo root) | the July v1 submission source |

`thesis_v2.tex` was removed in the commit that created this directory. It did
not compile: it held two concatenated documents, the second orphaned after
`\end{document}`, and neither its figure paths nor its bibliography resolved
from the repository root. Its first document is the ancestor of `thesis.tex`
and is recoverable from git history at `d1a847e`.

## Regenerating the exhibits

Figures come from `python scripts/plot_thesis_figures.py`; the copies in
`figures/` are what the document actually reads, so re-copy them after a
regeneration. Tables 2, 3 and 4 are generated by

```bash
python scripts/make_thesis_descriptives.py   # Tables 2 and 4
python scripts/make_thesis_correlations.py   # Table 3
```

which read the same parquets the regressions do and write to
`outputs/tables/thesis_*.csv`. The remaining tables are transcribed by hand
from `outputs/tables/` — the gate CSVs named in each table's notes.

## Register

The manuscript is written as a research paper, not as a revision of an earlier
one. It does not refer to previous versions of itself, to the review it
responds to, or to this repository. Where an earlier iteration's measurement
error is discussed, it is stated impersonally as a property of GDELT's GKG 1.0
stream, which is what makes it a finding rather than an apology. Keep that
register in any further edit: no first person, no address to the reader, and
no narration of how the document came to be.

The one deliberate exception is the pre-registration ordering, which the text
cites as verifiable. That is a methodological claim about when each test was
fixed relative to its data, and it is load-bearing. Note that **Gates 3, 4 and
5 are pre-registered and Gate 2 is not** — do not let an edit blur that, since
the paper invites the reader to check it.

Two further conventions worth preserving: paragraphs open with an unbolded
topic sentence (bold is reserved for defined terms and for key figures inside
tables), and p-values below 0.001 are given in scientific notation.

## The rule that applies here too

Every number in `thesis.tex` was checked against `outputs/tables/` on
2026-08-26. `outputs/tables/` is authoritative over any prose in `docs/`,
including `findings_status.md`, which was itself the stale side in two places.
Before changing a number here, read
[`docs/findings_status.md`](../../docs/findings_status.md) — six claims in this
project were retracted, and the documents reporting them are kept unedited.
